import VenueDetailsClient from "@/components/venue-details-client";

export default function VenueDetailPage({ params }: { params: { id: string } }) {
  return <VenueDetailsClient venueId={params.id} />;
}
